<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Email Verification</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f9f9f9;
            color: #333;
            margin: 0;
            padding: 0;
            text-align: center;
        }

        h1 {
            color: #3498db;
            margin-bottom: 20px;
        }

        p {
            margin: 10px 0;
            line-height: 1.6;
        }

        a {
            color: #2ecc71;
            text-decoration: none;
            font-weight: bold;
        }

        a:hover {
            text-decoration: underline;
        }

        .container {
            max-width: 600px;
            margin: 20px auto;
            padding: 20px;
            background-color: #fff;
            border-radius: 5px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
        }

        .btn-verify {
            display: inline-block;
            padding: 10px 20px;
            background-color: #2ecc71;
            /* Unique color for the button */
            color: #fff;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

        .btn-verify:hover {
            background-color: #27ae60;
            /* Hover effect color */
        }

        .footer {
            margin-top: 40px;
            color: #555;
            font-size: 14px;
        }

        .footer-text {
            margin-top: 10px;
            font-style: italic;
            color: #777;
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>Email Verification</h1>
        <p>Dear <%= userName %>,</p>
        <p>Thank you for signing up with Taskify! To complete the registration process, please click the button below to
            verify your email:</p>
        <a class="btn-verify" href="<%= verificationLink %>">Verify Email</a>
        <p>If you didn't sign up for our service, please ignore this email.</p>
    </div>

    <div class="footer">
        <p>Powered by Taskify</p>
        <div class="footer-text">Connecting People, Empowering Teams</div>
    </div>
</body>

</html>